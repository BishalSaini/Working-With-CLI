version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: echo "Hello, World!"

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
